'use strict';

import { combineReducers } from 'redux';

function Test(state = null, action) {
    switch (action.type) {
    default:
        return state;
    }
}

export default combineReducers({
    Test
});